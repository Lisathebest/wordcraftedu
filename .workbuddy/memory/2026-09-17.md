# 2026-09-17

## Git: 合并并推送 wordcraftedu

- 本地与 origin/main 分叉（远程有一套同内容但不同 hash 的提交，来自 codex 分支重写历史）。处理方式：`git merge --no-ff origin/main`，冲突仅出现在 `src/app/api/generate-illustration/route.ts` 与 `route.test.ts`，两边都取本地版本（env 可配置 + 5xx/408/429 重试 + 401/403 明确提示）。
- 合并结果树 == 本地 HEAD 树（远程独有内容本地已有，或本地有意删除：data/*.png 迁到 public/vocab、删掉 Project-photo.png、Business Plan docx、website-*.png、vocab card templete.indd）。
- 推送踩坑：`git push` 报 `RPC failed; HTTP 400 curl 22`。解决：`git -c http.postBuffer=524288000 -c http.version=HTTP/1.1 push origin main` 成功。
- 安全网：合并前建了备份分支 `backup/local-before-merge-20260917`（仍保留在本地）。
- 验证：`npx vitest run src/app/api/generate-illustration/route.test.ts` 4 passed。
